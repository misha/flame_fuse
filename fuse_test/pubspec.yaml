name: fuse_test
description: "Tests for the fuse package."
publish_to: "none"
version: 1.0.0+1
environment:
  sdk: ">=3.4.1 <4.0.0"
dependencies: 
  flutter:
    sdk: flutter
  flame: ^1.36.0
  flame_fuse:
    path: ../
  flutter_hooks: ^0.20.5
  provider: ^6.0.5
  shared_preferences: ^2.2.2
dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.11
  flutter_gen: ^5.5.0+1
  flutter_lints: ^6.0.0
  storybook_flutter: ^0.14.0
  storybook_flutter_test: any
flutter:
  uses-material-design: true
